export {
  formatPresignedRequest,
  resolveBooleanConfig,
  resolveEndpointUrl,
  resolveObjectStorageKey as resolveS3Key,
  encodeObjectStoragePath as encodeS3Path,
} from '../../object-storage/shared';
